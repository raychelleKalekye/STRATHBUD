<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Strathbud Locations</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;900&display=swap');
        .form4 {
                position: relative;
                width: 350px;
                height: auto;
                border-radius: 20px;
                padding: 40px;
                box-sizing: border-box;
                background: #ecf0f3;
                box-shadow: 14px 14px 20px #cbced1, -14px -14px 20px white;
                margin-left: 40%;
                margin-top:3%;
                margin-bottom:3%;

        }
        .data input{
           color: red;
           text-align: left;
           margin-top: 30px;
           background: #ecf0f3;
  padding: 10px;
  padding-left: 20px;
  height: 50px;
  font-size: 14px;
  border-radius: 50px;
  box-shadow: inset 6px 6px 6px #cbced1, inset -6px -6px 6px white;
        }
        .brand-logo {
            height: 100px;
            width: 100px;
            
            margin: auto;
            border-radius: 50%;
            box-sizing: border-box;
            box-shadow: 7px 7px 10px #cbced1, -7px -7px 10px white;
        }
        label, input, button {
            display: block;
            width: 100%;
            padding: 0;
            border: none;
            outline: none;
            box-sizing: border-box;
        }

            label {
            margin-bottom: 4px;
            }

            label:nth-of-type(2) {
            margin-top: 12px;
            }
        .brand-title {
            margin-top: 10px;
            font-weight: 400;
            font-size: 1.5rem;
            color: #1DA1F2;
            letter-spacing: 1px;
        }
        button {
            color: white;
            margin-top: 20px;
            background: #1DA1F2;
            height: 40px;
            border-radius: 20px;
            cursor: pointer;
            font-weight: 900;
            box-shadow: 6px 6px 6px #cbced1, -6px -6px 6px white;
            transition: 0.5s;
            }

            button:hover {
            box-shadow: none;
            }
            .locations-container {
    padding: 20px;
    border: 1px solid #ccc;
    border-radius: 10px;
    background-color: #f9f9f9;
}

.locations-container h2 {
    font-size: 24px;
    margin-bottom: 10px;
}

.locations-container p {
    font-size: 16px;
    margin-bottom: 20px;
}

.locations-list ul {
    list-style-type: none;
    padding: 0;
}

.locations-list li {
    font-size: 18px;
    margin-bottom: 10px;
}

    </style>
</head>
<body>
    <%- include ('reusables/header')%>
    <%- include ('reusables/menu')%>
    <% if (action === 'add') { %>
        <section>
            <form class="form4" action="/Strathbud/Locations/newLocation" method="POST">
                <p class="brand-title">Fill the form below to add a new location</p>
                <div class="data">
                    <label for="Location">Location:</label>
                    <input type="text" id="Location" name="Location">
                </div>
                <button type="submit">Submit</button>
            </form>
        </section>
    <% } else { %>
        <section>
            <div class="locations-container">
                <h2>Strathbud Locations</h2>
                <p>Hello Stratizens. Below are the available locations in school where you can host events, clubs, societies, and sports meetings.</p>
                <% if (locsData.length > 0) { %>
                    <div class="locations-list">
                        <ul>
                            <% locsData.forEach(function(data) { %>
                                <li><%= data['Location'] %></li>
                            <% }); %>
                        </ul>
                    </div>
                <% } else { %>
                    <p>No locations available.</p>
                <% } %>
            </div>
        </section>
    <% } %>
    <%- include ('reusables/footer')%>
</body>

</html>
